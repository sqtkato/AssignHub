<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.assignhub.mapper.DeletedEmployeeMapper">

	<resultMap id="deletedEmployeeResultMap"
		type="com.assignhub.entity.Employee">
		<id property="empId" column="emp_id" />
		<result property="lastName" column="last_name" />
		<result property="firstName" column="first_name" />
		<result property="lastNameKana" column="last_name_kana" />
		<result property="firstNameKana" column="first_name_kana" />
		<result property="hireDate" column="hire_date" />
		<result property="yearsOfService" column="years_of_servise" />
		<result property="BirthDate" column="birth_date" />
		<result property="zipCode" column="zip_code" />
		<result property="address1" column="address1" />
		<result property="address2" column="address2" />
		<result property="engineerType" column="engineer_type" />
		<result property="accountId" column="account_id" />
		<result property="companyId" column="company_id" />
		<result property="companyName" column="emp_company" />
		<result property="companyNameAssign"
			column="emp_assign_company" />
		<result property="department" column="department" />
		<result property="jobTitle" column="job_title" />
		<result property="empTel" column="emp_tel" />
		<result property="email" column="email" />
		<result property="deleteFlg" column="delete_flg" />
		<result property="createdAt" column="created_at" />
		<result property="updatedAt" column="updated_at" />
		<result property="contractStartDate"
			column="contract_start_date_assign" />
		<result property="contractEndDate"
			column="contract_end_date_assign" />
		<result property="unitPrice" column="unit_price_assign" />
		<result property="loginId" column="login_id" />
	</resultMap>
	<!-- 一覧表示 -->
	<select id="findAll" resultMap="deletedEmployeeResultMap">
		SELECT e.*,
		c.company_name AS emp_company,
		c2.company_name AS
		emp_assign_company
		FROM m_employee e
		LEFT JOIN m_company c
		ON
		e.company_id = c.company_id
		LEFT JOIN t_assignment a
		ON a.emp_id =
		e.emp_id
		AND a.contract_end_date is null
		LEFT JOIN m_company c2
		ON
		a.company_id = c2.company_id

		<where>
			e.delete_flg = 1
			<if test="empName != null and empName != ''">
				AND CONCAT(e.last_name, e.first_name) LIKE CONCAT('%',
				#{empName}, '%')
			</if>

			<if test="empAssignCompany != null and  empAssignCompany != ''">
				AND c2.company_name LIKE CONCAT('%', #{empAssignCompany},
				'%')
			</if>

			<if test="empCompany != null and  empCompany != ''">
				AND c.company_name LIKE CONCAT('%', #{empCompany}, '%')
			</if>

			<if test="empEngineerType != null and empEngineerType != ''">
				AND e.engineer_type LIKE CONCAT('%', #{empEngineerType},
				'%')
			</if>
		</where>

	</select>
	<!-- エクスポート用 -->
	<select id="findByIds" resultMap="deletedEmployeeResultMap">
		SELECT e.*,
		c.company_name AS company_name_belong,
		c2.company_name AS
		company_name_assign,
		ac.login_id AS login_id,
		a.contract_start_date AS
		contract_start_date_assign,
		a.contract_end_date AS
		contract_end_date_assign,
		a.unit_price AS unit_price_assign
		FROM
		m_employee e
		LEFT JOIN m_company c ON e.company_id = c.company_id
		LEFT
		JOIN t_assignment a ON a.emp_id = e.emp_id AND a.contract_end_date IS
		NULL
		LEFT JOIN m_company c2 ON a.company_id = c2.company_id
		LEFT JOIN
		m_account ac ON e.account_id = ac.account_id
		WHERE e.delete_flg = 1
		AND
		e.emp_id IN
		<foreach item="id" collection="ids" open="(" separator=","
			close=")">
			#{id}
		</foreach>

	</select>
	<!-- 復元 -->
	<update id="restore">
		UPDATE m_employee
		SET delete_flg = 0
		WHERE emp_id =
		#{id} AND delete_flg = 1
	</update>
	<!-- 一括復元 -->
	<update id="restoreBulk">
		UPDATE m_employee 
		SET delete_flg = 0
		WHERE emp_id IN
		<foreach item="id" collection="ids" open="(" separator=","
			close=")">
			#{id}
		</foreach>
		AND delete_flg = 1
	</update>
	<!-- 削除 -->
	<delete id="physicalDelete">
		DELETE FROM m_employee
		WHERE emp_id = #{id}
	</delete>
	<!-- 一括削除 -->
	<delete id="physicalDeleteBulk">
		DELETE FROM m_employee
		WHERE emp_id IN
		<foreach item="id" collection="ids" open="(" separator=","
			close=")">
			#{id}
		</foreach>
	</delete>

	<select id="countAccountsByEmpolyeeId" resultType="int">
		SELECT COUNT(*)
		FROM m_employee e
		INNER JOIN m_account a
		 ON e.account_id = a_account_id
		WHERE e.emp_id = #{id} 
		AND a.delete_flg = 0
	</select>

	<select id="countAccountsByEmpolyeeIds" resultType="int">
		SELECT COUNT(*)
		FROM m_employee e
		INNER JOIN m_account a
		 ON e.account_id = a_account_id
		WHERE e.emp_id IN
		<foreach item="id" collection="ids" open="(" separator=","
			close=")">
			#{id}
		</foreach>
		AND a.delete_flg = 0
	</select>

	<select id="countCompaniesByEmpolyeeId" resultType="int">
		SELECT COUNT(*)
		FROM m_employee e
		INNER JOIN m_company c
		 ON e.company_id = c.company_id
		WHERE e.emp_id = #{id} 
		AND c.delete_flg = 0
	</select>

	<select id="countCompaniesByEmpolyeeIds" resultType="int">
		SELECT COUNT(*)
		FROM m_employee e
		INNER JOIN m_company c
		 ON e.company_id = c.company_id
		WHERE e.emp_id IN
		<foreach item="id" collection="ids" open="(" separator=","
			close=")">
			#{id}
		</foreach>
		AND c.delete_flg = 0
	</select>

	<select id="countAssignmentsByEmpolyeeId" resultType="int">
		SELECT
		COUNT(*)
		FROM t_assignment
		WHERE emp_id = #{id}
	</select>

	<select id="countAssignmentsByEmpolyeeIds" resultType="int">
		SELECT COUNT(*)
		FROM t_assignment a
		WHERE emp_id IN
		<foreach item="id" collection="ids" open="(" separator=","
			close=")">
			#{id}
		</foreach>
	</select>

</mapper>      