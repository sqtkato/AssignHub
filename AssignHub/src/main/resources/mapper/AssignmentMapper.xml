<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.assignhub.mapper.AssignmentMapper">

	<update id="deleteByCompanyId">
		UPDATE t_assignment
		SET delete_flg = 1,
		updated_at = NOW()
		WHERE company_id = #{id}
		AND delete_flg = 0
	</update>

	<update id="deleteBulkByCompanyId">
		UPDATE t_assignment
		SET delete_flg = 1,
		updated_at = NOW()
		WHERE company_id IN
		<foreach collection="ids" item="id" open="(" separator=","
			close=")">
			#{id}
		</foreach>
	</update>

</mapper>